# Глобальный словарь для маппинга short_id -> telegram_file_id
# ✅ Это нормально для небольших нагрузок (т.е. для вашего случая)
# ✅ Данные хранятся только во время работы бота
# ✅ При перезагрузке бота - очищается (это нормально)

pending_callbacks: dict[str, str] = {}

# Если хотите ДОПОЛНИТЕЛЬНО логировать в файл:
import logging
logger = logging.getLogger(__name__)

def log_cache_state():
    """Логирование состояния кэша"""
    logger.info(f"📊 Cache state: {len(pending_callbacks)} items")
    for short_id, file_id in pending_callbacks.items():
        logger.debug(f"  - {short_id}: {file_id[:20]}...")
